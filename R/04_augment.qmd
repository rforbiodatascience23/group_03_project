---
title: "04_augment.qmd"
format: html
editor: visual
---

## Augment

This document takes care of the augment of the data. In the following the data will be log2 transformed.

## Set up

Loading required packages.

```{r}
#| message: false
library("dplyr")
```

Load the clean data.

```{r}
#| label: load data
#| message: false
dat_clean <- read_tsv(file = "../data/02_dat_clean.tsv")

```

## Normalization

## Transformation

In the following the data will be log2 transformed.

```{r}
#| label: log-transformation

dat_aug <- dat_clean |>
  #mutate_at(vars(starts_with("ILMN")), log2)
  mutate(across(starts_with("ILMN"), log2))  # Dette er væsentlig hurtigere

```

Writes a tsv file containing the augment data.

```{r}
#| label: write tsv
dat_aug |> 
  write_tsv(file = "../data/04_dat_aug.tsv")

```
